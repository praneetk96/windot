/*  ____ _  __
 * |  _ \ |/ /
 * | |_)| ' /   %AUTHOR%
 * |  __/ . \   https://www.gitlab.com/praneetk96
 * |_|  |_|\_\
 *
 * Codechef's problem code: %UPPERFILE%
 * Date: %DATE%
*/

#include<bits/stdc++.h>
using namespace std;
#define all(x) x.begin(), x.end()
#define floop(a,b) for(int i=a;i<b;i++)
#define floop1(a,b) for(int i=a;i<=b;i++)
#define floop2(a,b) for(int i=a;i>=b;i--)
#define printyn(p) { if(p) std::cout << "Yes" << std::endl; else std::cout << "No" << std::endl; }
#define printYN(p) { if(p) std::cout << "YES" << std::endl; else std::cout << "NO" << std::endl; }
typedef long long ll;
template <typename T> bool chmin(T& a, const T& b) { return (b < a) ? (a = b, true) : false; }
template <typename T> bool chmax(T& a, const T& b) { return (a < b) ? (a = b, true) : false; }
using vi = vector<int>;
using vc = vector<char>;
using vs = vector<string>;

int solve() {
    return 0;
}

int main() {
    ll testCases;
    std::cin >> testCases;
    while(testCases--) {
        solve();
    }
    return 0;
}
